# Framework Package: separate-engine-from-domain

This is a sibling change to the engine-level change tracking the same refactoring across layers.

**Main change:** `docs/context/openspec/changes/separate-engine-from-domain/`

This package-local change tracks framework-specific tasks for the refactoring:
- Create framework package structure
- Move shared framework steps
- Create framework specs and documentation
- Move framework tests

All sibling changes use the same slug (`separate-engine-from-domain`) for coordination.

See the main engine-level change for full context, design, and cross-layer task coordination.
