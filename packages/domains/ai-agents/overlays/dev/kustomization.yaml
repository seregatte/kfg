apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

# Development overlay for AI agents domain
# References framework and domain manifests, adds dev-specific resources

resources:
  - ../../../../framework/
  - ../../manifests/
  - agents-workflow.yaml
