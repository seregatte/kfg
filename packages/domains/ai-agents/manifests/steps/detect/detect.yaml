apiVersion: kfg.dev/v1alpha1
kind: Step
metadata:
  name: ai.steps.detect
spec:
  run: |
    echo "$AGENT"
    __kfg_log_debug "step:detect-agent" "Agent: $AGENT"
  output:
    name: AGENT
    type: string
