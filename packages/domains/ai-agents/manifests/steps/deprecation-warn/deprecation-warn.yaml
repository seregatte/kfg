apiVersion: kfg.dev/v1alpha1
kind: Step
metadata:
  name: ai.steps.deprecation-warn
spec:
  run: |
    __kfg_log_warn "The overlays/dev/ overlay is deprecated. Use 'kfg ai' to generate a project-specific configuration instead."
