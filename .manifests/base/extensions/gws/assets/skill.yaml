apiVersion: kfg.dev/v1alpha1
kind: Assets
metadata:
  name: kfg.extension.gws.skill
spec:
  input:
    format: yaml
  data:
    name: gws
    description: Google Workspace skill for integration with Google services
    type: skill
    content: |
      Google Workspace integration provides access to Google Drive, Gmail, and Calendar.
      
      Setup requires authentication credentials configured in your Google Cloud project.
