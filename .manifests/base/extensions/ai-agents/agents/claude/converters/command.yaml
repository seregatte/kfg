apiVersion: kfg.dev/v1alpha1
kind: Converter
metadata:
  name: ai.claude.conv.command
spec:
  input:
    format: yaml
  engine:
    expression: |
      "---\nname: " + .name + "\ndescription: " + .description + "\n---\n\n" + .prompt
  output:
    format: raw
