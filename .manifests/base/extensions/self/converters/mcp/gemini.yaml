apiVersion: kfg.dev/v1alpha1
kind: Converter
metadata:
  name: kfg.convert.self.mcp.gemini
spec:
  input:
    format: yaml
  engine:
    expression: |
      (.name) as $n | {($n): {"command": .server.command, "args": .server.args}}
  output:
    format: json
