apiVersion: kfg.dev/v1alpha1
kind: Step
metadata:
  name: kfg.detect-agent
spec:
  run: |
    echo "$AGENT"
    _kfg.log.debug "step:detect-agent" "Agent: $AGENT"
  output:
    name: AGENT
    type: string
