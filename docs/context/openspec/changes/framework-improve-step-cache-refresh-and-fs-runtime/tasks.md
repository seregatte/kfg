## 1. Framework adoption

- [ ] 1.1 Update `kfg.materialize` to execute nested `kfg` conversions through the runtime's internal execution helper.
- [ ] 1.2 Add or update framework validation covering unchanged materialize output and suppressed child startup logs.

## 2. Documentation

- [ ] 2.1 Update framework OpenSpec deltas and any touched framework docs for the runtime helper dependency and quiet nested execution behavior.
