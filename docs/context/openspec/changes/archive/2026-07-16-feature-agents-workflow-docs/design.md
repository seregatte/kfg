## Context

The current AGENTS.md has a Git Worktree Workflow section that covers worktree setup but stops before the OpenSpec cycle. The Feature Development Workflow section needs to bridge worktree creation → OpenSpec artifacts → implementation → archive → PR.

## Goals / Non-Goals

**Goals:**
- Document the complete per-feature cycle
- Clarify PR target rules (originating branch, not always main)
- Add CLI caveat
- Fix stale file path

**Non-Goals:**
- Not changing the Versioning Policy or Release Process sections
- Not restructuring existing sections unnecessarily
