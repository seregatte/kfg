## 1. ctx7 artifact discovery

- [ ] 1.1 Update `ctx7.steps.install` to discover new top-level children under `OUTPUT_DIR` through runtime filesystem snapshot/diff helpers.
- [ ] 1.2 Remove redundant ctx7 workflow artifact declarations once the Step registers produced skill paths dynamically.
- [ ] 1.3 Add or update domain validation covering ctx7 install artifact registration and cacheable restore behavior.

## 2. Documentation

- [ ] 2.1 Update domain OpenSpec deltas and any touched docs for ctx7's dynamic artifact discovery contract.
